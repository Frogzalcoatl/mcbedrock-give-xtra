Make sure entity selector comes after JSON argument in command. Will make it easier for user.

Replaceitem function

Fix interface type checker functions with range of key counts.
	Technically an object within required amount keys containing random ones that are not a part of the interface would be accepted.
